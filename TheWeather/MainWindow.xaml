<Window x:Class="TheWeather.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheWeather"
        mc:Ignorable="d"
        Title="MainWindow" 
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        Height="150" Width="200"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="MainWindow_OnMouseLeftButtonDown">
   
        <Border CornerRadius="10">
            <Border.Background >
                    <ImageBrush  ImageSource="{Binding weatherInfo.WeatherFirst}" Stretch="Uniform" >
                    </ImageBrush>
                    <!-- <ImageBrush ImageSource="./Assets/bg-image1.jpg" Stretch="Fill" /> -->
            </Border.Background>
             <Border CornerRadius="20"
                        BorderThickness="1"
                        Opacity="0.95"
                    >
                 <Grid ZIndex="999999">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Grid.Row="0" Foreground="White" Text="{Binding weatherInfo.CityInfo}"  TextAlignment="Center" HorizontalAlignment="Left" Margin="2"  ></TextBlock>

                            <StackPanel Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" Orientation="Horizontal">
                                <TextBlock Width="70" FontSize="34" Foreground="White" Text="{Binding weatherInfo.Degree}"></TextBlock>
                                <TextBlock Width="30" FontSize="14" Margin="-15,20,0,0" FontWeight="Bold" Foreground="White" Text="{Binding weatherInfo.Weather}"></TextBlock>
                            </StackPanel>
                     <TextBlock Grid.Column="0" Grid.Row="2" Foreground="White" Text="{Binding weatherInfo.WindDirectionName}"  TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="2"  ></TextBlock>
                         </Grid>      
            </Border>
        </Border>
        <Window.ContextMenu>
                <ContextMenu>
                        <MenuItem Header="隐藏" Click="MenuItem_OnClick_Hide"/>
                        <MenuItem Header="退出" Click="MenuItem_OnClick"/>
                </ContextMenu>
        </Window.ContextMenu>
</Window>
